import { ColumnSizeDirective } from './column-size.directive';

describe('ColumnSizeDirective', () => {
  it('should create an instance', () => {
    const directive = new ColumnSizeDirective();
    expect(directive).toBeTruthy();
  });
});
